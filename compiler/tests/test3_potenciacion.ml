/* Test 3 - Potenciación y expresiones complejas */
MAIN{
    DEC base, exp, resultado;
    INPUT base;
    INPUT exp;
    resultado = base ** exp;
    OUTPUT resultado;

    /* Expresión más compleja */
    resultado = (2 + base) ** 3;
    OUTPUT resultado
}
